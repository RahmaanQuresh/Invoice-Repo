import { describe, it, expect } from "vitest";

describe("DeathFear Phase 1 - Placeholder", () => {
  it("should pass a basic sanity check", () => {
    expect(1 + 1).toBe(2);
  });

  it("should have the correct app name", () => {
    const appName = "DeathFear";
    expect(appName).toBe("DeathFear");
  });
});
